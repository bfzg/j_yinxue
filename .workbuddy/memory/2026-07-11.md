# 2026-07-11 Work Log

## 抖音视频转播客音频 - 方案规划
- 项目: 九哥隐学原创播客小程序 (j_yinxue)
- 需求: 从抖音用户主页扒取视频 → 提取音频 MP3 → 生成中文字幕
- 抖音用户主页 URL: https://www.douyin.com/user/MS4wLjABAAAA7bhEyXsPq05h36FhPyW1JgIu0yHkMCJ2nPnLOlAUhA0UHMxSx3EGv5tmYAYAFoEL
- 用户确认: 只保留音频(删视频), 字幕用 medium 模型, 可以提供抖音 Cookie

## 技术方案确定
- 爬取: Python f2 库 (需要用户提供抖音登录 Cookie)
- 音频提取: ffmpeg (需 brew install)
- 字幕生成: faster-whisper (medium 模型, 中文)
- 脚本目录: scripts/douyin_to_podcast/

## Python 环境配置
- 将 WorkBuddy 自带的 Python 3.13.12 配置为系统可用
- 修改 ~/.zshrc, 添加 WORKBUDDY_PYTHON 环境变量和 PATH
- 路径: $HOME/.workbuddy/binaries/python/versions/3.13.12/bin
- 验证通过: python3 / pip3 在新终端中可用

## ffmpeg 环境配置
- 用户从 evermeet.cx 下载了 Intel 版 (x86_64), 无法在 Apple Silicon 上运行 (无 Rosetta 2)
- 从 osxexperts.net 下载了 ARM 原生版 ffmpeg 8.1
- 路径: /Volumes/cc/dev/ffmpeg/ffmpeg
- 已执行: xattr 去隔离 + codesign 签名
- 已添加到 ~/.zshrc PATH (FFMPEG_HOME 变量)
- 验证通过: ffmpeg 在新终端中可用

## 抖音爬虫脚本开发
- 目录: scripts/douyin/ (不是 douyin_to_podcast)
- Python venv: scripts/douyin/.venv (用 WorkBuddy Python 3.13 创建)
- 已安装: f2 0.0.1.7 (pip install, 之前后台安装失败/被OOM kill, 重新单独安装成功)
- 还未安装: faster-whisper (字幕模块, 暂时不需要)
- 已写代码: config.py, scraper.py, extractor.py, transcriber.py, main.py
- 修复 scraper.py bug: _find_f2_binary 返回的字符串含空格被当作单一文件路径, 改为返回 list[str]
- 修复 f2 CLI 参数: -k 传cookie字符串(不是文件), -M 指定模式(post), -o 指定最大下载数, -f false 不建子目录
- 修复 config.py get_cookie(): 过滤注释行(#开头)和空行
- 添加 main.py --max 参数: 命令行覆盖 MAX_VIDEOS, 方便测试
- 测试成功: 用户填好 Cookie 后 f2 CLI 模式下载成功, 视频保存在 output/videos/douyin/post/天涯神贴隐学/
- f2 忽略了 -o 2 限制, 下载了十几个文件(部分为 .tmp 未完成)
- 修复 main.py step_extract/step_transcribe: glob 改为 rglob 递归查找, 因为 f2 创建了子目录结构
- 下一步: 运行 --step extract 提取音频

## 2026-07-12 补充
- 音频提取成功: 13 个 MP3 文件已生成在 output/audio/
- 添加音量增益: extractor.py 加入 loudnorm 滤镜 (I=-14 LUFS, TP=-1.5dB)
- 批量增益: main.py --step boost 对已有 MP3 批量 loudnorm (13/13 成功)
- 生成 playlist.json: main.py --step playlist 自动解析文件名提取标题/日期, URL 编码 COS 路径
- COS 桶为私有 (403 Forbidden), 需要签名 URL 才能访问
- 新生成的 playlist.json 使用不带签名的永久 URL (需将 COS 桶设为公有读才能播放)
- 新增 main.py 步骤: --step boost (音量增益), --step playlist (生成播放列表)
